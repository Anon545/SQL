Группы команд: data definition language (DDL) и data manipulation language (DML).
	DDL и DML - это просто наборы команд, ничего более. Не языки.

data definition language (DDL) - команды, которые направлены на изменение структуры базы данных. Создание таблицы, изменение её элементов, структуры.
data manipulation language (DML) - команды позволяют изменять данные. Добавление, изменение, удаление данных.
- - - - - --
Каким же образом данные изменяются и сохраняются в базе? те изменения, которые вносим мы, изначально видны только пользователю (нам). Чтобы эти изменения увидели все, используют команду Commit.
	Commit - сохраняет все изменения, которые мы совершим. Позволяет увидеть изменения всем пользователям БД.
- - - - - -
после команды Commit все изменения перейдут в БД. Откатиться к предыдущей версии не будет возможности. Если пользователь совершил ошибку, то её лучше найти до команды Commit.
Если мы совершили ошибку и ещё не воспользовались командой Commit, то можно прожать команду Rollback.
	Rollback - позволяет откатиться к предыдущей версии БД,  на предыдущий Commit.
- - - - - -
Команды DDL содержат в себе Commit по умолчанию. Если создать таблицу, добавить в неё записи, то откатить это не получится.
